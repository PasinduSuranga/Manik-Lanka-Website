import ContactClient from "./ContactClient";

export const metadata = {
  title: 'Contact Us | Manik Lanka Holidays',
  description: 'Get in touch with Manik Lanka Holidays to plan your custom Sri Lanka tour, ask questions about our packages, or book your next adventure.',
};

export default function ContactPage() {
  return <ContactClient />;
}