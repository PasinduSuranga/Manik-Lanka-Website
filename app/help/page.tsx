import HelpClient from "./HelpClient";

export const metadata = {
  title: 'Help & FAQ | Manik Lanka Holidays',
  description: 'Find answers to frequently asked questions about booking tour packages, payment methods, cancellation policies, and more with Manik Lanka Holidays.',
};

export default function HelpPage() {
  return <HelpClient />;
}